데이터 타입                        자바스크립트                                          타입스크립트
----------------------------------------------------------------------------------------------------------------------------------------
원시데이터타입                     숫자(number), 문자(string), 불린(boolean)              자바스크립트와 동일
(Prmitive type)                   null, undefined ..
----------------------------------------------------------------------------------------------------------------------------------------
참조 데이터타입                    클래스(Class) , 함수(Function),                        클래스(Class) , 함수(Function),
(Reference type)                  배열(Array), 객체(Obejct, {})..                        배열(Array), 객체(Obejct, {})
= 객체 타입(Object type)                                                                 인터페이스(Interface), 타입(Type)
----------------------------------------------------------------------------------------------------------------------------------------
튜플(Tuple)                        지원 안됨                                             지원됨 : Array(배열) + 타입
----------------------------------------------------------------------------------------------------------------------------------------
열거형, Enum(Enumeration)          지원 안됨                                             지원됨 : Object 타입 형식, 나열
----------------------------------------------------------------------------------------------------------------------------------------
제너릭(Generic)                    지원 안됨                                             지원됨 : 파라미터의 데이터 타입을 객체로 정의하는 경우
----------------------------------------------------------------------------------------------------------------------------------------
Type Alias(타입별칭)               지원 안됨                                             지원됨 : 타입의 별칭 정의
----------------------------------------------------------------------------------------------------------------------------------------
Union Type(유니온 타입)            지원 안됨                                             지원됨 
----------------------------------------------------------------------------------------------------------------------------------------
실행방식                           런타임 형식으로 바로 실행                              TS코드 -> 컴파일 -> JS 코드생성 -> 런타임
----------------------------------------------------------------------------------------------------------------------------------------